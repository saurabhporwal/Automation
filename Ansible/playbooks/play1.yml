---
- name: Transfer and execute a script.
  hosts: anstwo
  user: root
  sudo: yes
  tasks:
     - name: Transfer the script
       copy: src=/data/Ansible/playbooks/testexec.sh dest=/data/remote/testexec.sh mode=0777

     - name: Execute the script
       command: sh /data/remote/testexec.sh
       become: true
       become_user: root
